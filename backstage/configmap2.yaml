apiVersion: v1
kind: ConfigMap
metadata:
  name: backstage-config-production
  namespace: backstage
data:
  app-config.production.yaml: |
    # Configurações específicas para produção
    auth:
      providers:
        guest:
          enabled: false